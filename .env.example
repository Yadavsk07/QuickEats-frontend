# Local development (copy to .env and keep .env out of git)
# For Netlify: set these in Site settings → Environment variables (use your Render backend URL)

VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000

# Production (Netlify) – replace with your Render backend URL, e.g.:
# VITE_API_URL=https://your-app-name.onrender.com/api
# VITE_SOCKET_URL=https://your-app-name.onrender.com
