# Netlify build for frontend (use Base directory = "frontend" in Netlify if repo root contains backend + frontend)
[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
